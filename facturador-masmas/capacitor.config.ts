import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'conjuntosolucion.facturador',
  appName: 'facturador-masmas',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
