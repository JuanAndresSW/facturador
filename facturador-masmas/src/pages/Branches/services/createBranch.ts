import ajax from 'interceptors/ajax';
import branch from '../models/branch';
import Valid from 'utilities/Valid';

export default function createPointOfSale(point: branch, callback: Function): void {
    
}