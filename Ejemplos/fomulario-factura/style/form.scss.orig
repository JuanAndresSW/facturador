@import 'definition';

body {
    @include purpleBack();
    @include globalFont();
}

header {
    margin: auto;
    text-align: center;
    h1 {
        font-size: 4rem;}
    p {
        margin-top: -2rem;
        font-size: 2rem;
    }
}

form {
    @include purplePanel();
    @include neatBorder();
    max-width: $formMax;
    margin: 2rem auto 2rem auto;
    padding: 2rem;

    font-size: 2rem;

    div {
        margin: 0 auto 2rem auto;
        .inputControl {
            display: block;
            width: 100%;
            height: 2rem;
            padding-left: 0.5rem;
            font-size: 1rem;
            color: $darkGray;
            @include grayBorder();
        }

        input[type="checkbox"], input[type="radio"] {
            display: inline-block;
            margin-right: 1rem;
            min-height: 1rem;
        }

        option {
            color: $darkGray;
        }

        label {
            display: block;
            margin-bottom: 0.5rem;
            font-size: 1.5rem;
            span {
                font-size: 1rem; color: $gentleGray;
            }
        }

    }

    #productos {
        @include neatBorder();
        padding: 2px 10px 30px 10px;
    
        display: grid;
        grid-template-columns: 1fr 2fr 1fr;
        gap: 20px;  

        label {
            text-align: center;
            border-bottom: 1px solid $darkGray;
            padding: 10px 5px;
            margin-bottom: 0;
        }

        input {
            width: 90%;
            margin: 0 auto;
            font-size: 1.5rem;
        }
    }

    #btn-add-row {
        display: block;
        margin: -50px auto 0 auto;
        width: 40px;
        height: 40px;
        background: center / contain url("../img-plus-sign.png") whitesmoke;
        border: none;
        border-radius: 50%;
    }
    #btn-add-row:hover {
        background-color: $gentleLavanda;
    }

    button#submit {
        background-color: rgb(43, 180, 16);
        width: 100%;
        height: 3rem;
        margin-top: 1rem;
        font-size: 1.8rem;
        border: none;
        border-radius: 5px;
        color: whitesmoke;
    }
    button#submit:hover {
        background-color: rgb(60, 219, 28);
    }
}